import { IscompletePipe } from './iscomplete.pipe';

describe('IscompletePipe', () => {
  it('create an instance', () => {
    const pipe = new IscompletePipe();
    expect(pipe).toBeTruthy();
  });
});
